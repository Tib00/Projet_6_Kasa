import React from 'react';
import ReactDOM from 'react-dom/client';
import Routage from './pages/routage/routage';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Routage/>
  </React.StrictMode>
);
